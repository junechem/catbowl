# Core - needed everywhere (Pi and workstation)
numpy>=1.24
opencv-python-headless>=4.8    # use opencv-python instead if you want GUI windows
PyYAML>=6.0
scikit-learn>=1.3
joblib>=1.3

# Inference backend. torch is the default; on a Pi 4 the CPU wheels are fine.
# Swap for ai-edge-litert if you would rather run a .tflite model.
torch>=2.2
torchvision>=0.17

# Raspberry Pi hardware only - install with: pip install -r requirements-pi.txt
