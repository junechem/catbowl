-r requirements.txt

# PCA9685 servo driver over I2C (actuator.driver: pca9685).
#
# smbus2, not adafruit-circuitpython-servokit. servokit pulls in Blinka, which
# pulls in lgpio, which PyPI ships only as a source distribution linking against
# a C library Debian does not package - so it cannot be built on Raspberry Pi OS
# at all. smbus2 is pure Python and talks to /dev/i2c-1 directly; see
# catbowl/pca9685.py.
smbus2>=0.4

# Servos wired straight to header pins (actuator.driver: gpio)
pigpio>=1.78

# CSI ribbon cameras (camera.device: "csi:0"). On Raspberry Pi OS this is better
# installed with apt: sudo apt install -y python3-picamera2
# picamera2>=0.3.12
